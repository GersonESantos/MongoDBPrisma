generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma.js"
}

datasource db {
  provider = "mongodb"
}

model User {
  id    String @id @default(auto()) @map("_id") @db.ObjectId
  age   String
  email String @unique
  name  String
}
